apiVersion: v1
kind: Secret
metadata:
  name: physioflow-secrets
type: Opaque
stringData:
  # PostgreSQL (in-cluster)
  POSTGRES_PASSWORD: "PhysioFlow_Dev_2026!"
  DATABASE_URL: "postgresql://physioflow:PhysioFlow_Dev_2026!@physioflow-postgres:5432/physioflow"

  # Keycloak API secret (generate after realm import)
  KEYCLOAK_CLIENT_SECRET: "physioflow-api-dev-secret"

  # Redis (optional - can use k3s Redis if available)
  REDIS_URL: ""

  # MinIO/S3 (using Unraid MinIO)
  S3_ENDPOINT: "https://unraid-s3.trancloud.work"
  S3_ACCESS_KEY: "admin"
  S3_SECRET_KEY: "ycm&17XK"
  S3_BUCKET: "physioflow-dev"
