apiVersion: v1
kind: Secret
metadata:
  name: physioflow-secrets
type: Opaque
stringData:
  # PostgreSQL (in-cluster)
  POSTGRES_PASSWORD: "PhysioFlow_Prod_2026_Secure!"
  DATABASE_URL: "postgresql://physioflow:PhysioFlow_Prod_2026_Secure!@physioflow-postgres:5432/physioflow"

  # Keycloak API secret (generate after realm import)
  KEYCLOAK_CLIENT_SECRET: "physioflow-api-prod-secret"

  # Redis
  REDIS_URL: ""

  # MinIO/S3 (using Unraid MinIO)
  S3_ENDPOINT: "https://unraid-s3.trancloud.work"
  S3_ACCESS_KEY: "admin"
  S3_SECRET_KEY: "ycm&17XK"
  S3_BUCKET: "physioflow-prod"
