# =============================================================================
# EMR Docker Development Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed
# DO NOT commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------
POSTGRES_USER=emr
POSTGRES_PASSWORD=emr_secret_dev_only
POSTGRES_DB=physioflow
POSTGRES_PORT=5432

# Keycloak database (created by init script)
KEYCLOAK_DB=keycloak

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Keycloak Configuration
# -----------------------------------------------------------------------------
KEYCLOAK_PORT=8180
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin_secret_dev_only

# Realm configuration directory (for development imports)
KEYCLOAK_REALM_DIR=./keycloak/realms

# PhysioFlow OAuth Configuration
PHYSIOFLOW_WEB_URL=http://localhost:3000
KEYCLOAK_API_CLIENT_SECRET=physioflow-api-secret-change-in-production

# -----------------------------------------------------------------------------
# MinIO (S3-Compatible Storage) Configuration
# -----------------------------------------------------------------------------
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=minio_secret_dev_only

# Default buckets
MINIO_DEFAULT_BUCKET=emr-documents
MINIO_ATTACHMENTS_BUCKET=emr-attachments

# -----------------------------------------------------------------------------
# Meilisearch Configuration
# -----------------------------------------------------------------------------
MEILISEARCH_PORT=7700
MEILI_MASTER_KEY=meili_master_key_dev_only_change_in_prod

# -----------------------------------------------------------------------------
# Development Tools (docker-compose.dev.yml)
# -----------------------------------------------------------------------------
# Mailhog - Email testing
MAILHOG_SMTP_PORT=1025
MAILHOG_UI_PORT=8025

# Adminer - Database management UI
ADMINER_PORT=8081

# Redis Commander - Redis management UI
REDIS_COMMANDER_PORT=8082

# Development SQL scripts directory
DEV_SQL_SCRIPTS=./dev-scripts

# -----------------------------------------------------------------------------
# Application Configuration (for reference)
# -----------------------------------------------------------------------------
# These are used by the application, not Docker services directly

# Database connection string
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# Redis connection string
# REDIS_URL=redis://localhost:${REDIS_PORT}

# MinIO endpoint
# S3_ENDPOINT=http://localhost:${MINIO_API_PORT}
# S3_ACCESS_KEY=${MINIO_ROOT_USER}
# S3_SECRET_KEY=${MINIO_ROOT_PASSWORD}

# Meilisearch endpoint
# MEILISEARCH_URL=http://localhost:${MEILISEARCH_PORT}
# MEILISEARCH_KEY=${MEILI_MASTER_KEY}

# Keycloak endpoint
# KEYCLOAK_URL=http://localhost:${KEYCLOAK_PORT}
# KEYCLOAK_REALM=physioflow
# KEYCLOAK_CLIENT_ID=physioflow-api
# KEYCLOAK_SECRET=${KEYCLOAK_API_CLIENT_SECRET}

# -----------------------------------------------------------------------------
# Web Application Environment Variables (Next.js)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_API_URL=http://localhost:8080/api
# NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:${KEYCLOAK_PORT}
# NEXT_PUBLIC_KEYCLOAK_REALM=physioflow
# NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=physioflow-web
# NEXT_PUBLIC_APP_URL=http://localhost:3000
